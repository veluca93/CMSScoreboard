<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.cms_dev.cmsscoreboard" >
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/cms"
        android:logo="@mipmap/cms"
        android:label="@string/app_name"
        android:theme="@style/Theme.CMSTheme">
        <service android:name=".ScoreboardService" />
        <activity
            android:name=".ScoreboardActivity"
            android:label="@string/title_activity_scoreboard" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddScoreboardActivity" android:label="@string/title_activity_add_scoreboard">
        </activity>
    </application>

</manifest>
